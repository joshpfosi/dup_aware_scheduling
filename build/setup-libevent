#! /bin/bash
#
# Install libevent

cd /usr/local/comp112
unzip -u libevent-master
cd libevent-master

# NOTE: If any of these steps for inexplicable reasons, a troubleshooting
# technique which worked for me was removing the libevent directory,
# recopying the zip file and unziping it.

sh autogen.sh
./configure
make
sudo make install
sudo ln -sf /usr/local/lib/libevent-2.1.so.5 /usr/lib/libevent-2.1.so.5
